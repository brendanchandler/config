# Emacs Config

## Setup

Add the following to `~/.config/emacs/init.el` (or `~/.emacs.d/init.el`):

```elisp
(load "~/src/config/emacs/init.el")
```

This will load the config from this repository, which in turn loads the modules in `lisp/`.
