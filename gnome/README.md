# GNOME Keyboard Shortcuts

Custom GNOME keyboard shortcut configuration stored as `dconf` dumps.

## Usage

Apply shortcuts to the system:
```bash
./apply-shortcuts.sh
```

Save current shortcuts after making changes in GNOME Settings:
```bash
./save-shortcuts.sh
```

Then commit the updated `.ini` files.
